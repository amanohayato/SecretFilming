#pragma once

#include "../../../../Math/Vector2.h"

// スコアシートの紙の描画座標
static const Vector2 DOCUMENTS_POS				= Vector2(50.0f, 50.0f);
// スコアシートの紙の中心座標
static const Vector2 DOCUMENTS_CENTER			= Vector2(350.0f, 500.0f);

// スコアシートの見出しの描画座標
static const Vector2 HEADLINE_SPRITE_POS		= Vector2(100.0f, 110.0f);

// “確定度”の文字の描画座標
static const Vector2 AWARENESS_POS				= Vector2(80.0f, 270.0f);
// 確定度の数字の描画座標
static const Vector2 AWARENESS_NUMBER_POS		= Vector2(500.0f, 270.0f);

// “写真枚数”の文字の描画座標
static const Vector2 PHOTO_COUNT_POS			= Vector2(80.0f, 370.0f);
// 写真枚数の数字の描画座標
static const Vector2 PHOTO_COUNT_NUMBER_POS		= Vector2(500.0f, 370.0f);

// “逃走回数”の文字の描画座標
static const Vector2 ESCAPE_COUNT_POS			= Vector2(80.0f, 470.0f);
// 逃走回数の数字の描画座標
static const Vector2 ESCAPE_COUNT_NUMBER_POS	= Vector2(500.0f, 470.0f);

// “評価”の文字の描画座標
static const Vector2 EVALUATION_POS				= Vector2(80.0f, 570.0f);
// ランクの英語の描画座標
static const Vector2 EVALUATION_WORD_POS		= Vector2(280.0f, 650.0f);

